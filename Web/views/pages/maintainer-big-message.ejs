<!DOCTYPE html>
<html>
<head>
	<title>BigMessage - <%= serverData.name %> Maintainer Console</title>
	<% include ../partials/head %>
</head>
<body>
	<% include ../partials/donate %>
	<% include ../partials/dashboard-header %>
		
	<section class="section is-white">
		<div class="container">
			<div class="columns">
				<div id="menu" class="column is-one-quarter">
					<% include ../partials/maintainer-menu %>
				</div>
				<div class="column">
					<h1 class="title">
						BigMessage
					</h1>
					<div class="container">
						<article class="message is-primary">
							<div class="message-body">
								<div class="content">
									BigMessage is for <strong>really important announcements</strong>. This message is sent to every single server the bot is on. Use it only if it's absolutely necessary.
								</div>
							</div>
						</article>
					</div>
					<br>
					<div class="container">
						<form id="form" action="<%= currentPage %>?svrid=maintainer" method="post">
							<p class="control">
								<textarea name="message" class="textarea" maxlength="2000" placeholder="Message to send, in markdown"></textarea>
								<span class="help">This message will be sent to <strong>all <%= serverCount %> server<%= serverCount==1 ? "" : "s" %></strong>, so make sure it's important!</span>
							</p>
							<br>
							<% var formButtonsUnsaved = false; %>
							<% include ../partials/form-buttons %>
							<div id="form-buttons" style="display:none;background-color:#f5f7fa;position: fixed;padding: 20px;left: 50%;transform: translate(-50%, 0%);bottom: 0px;z-index: 100;width: 100%;">
								<div class="container">
									<% var formButtonsUnsaved = true; %>
									<% include ../partials/form-buttons %>
								</div>
							</div>
						</form>
					</div>
				</div>
			</div>
		</div>
	</section>

	<% include ../partials/footer %>
	<% include ../partials/scroll-top-button %>
</body>
</html>