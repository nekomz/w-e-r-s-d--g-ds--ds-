<!DOCTYPE html>
<html>
<head>
	<title>Commands - <%= serverData.name %> Maintainer Console</title>
	<% include ../partials/head %>
	<script src="/js/form-change-listener.js"></script>
</head>
<body onload="loadBackground();saveFormState();">
	<% include ../partials/dashboard-header %>
		
	<section class="section is-white">
		<div class="container">
			<div class="columns" id="frame">
				<div class="column is-one-quarter">
					<% include ../partials/maintainer-menu %>
				</div>
				<div class="column">
					<h1 class="title">
						Command Options
					</h1>
					<div class="container">
						<article class="message is-primary">
							<div class="message-body">
								<div class="content">
									These are command-related options in the <strong>config file</strong>, so you can edit them without restarting the bot.
								</div>
							</div>
						</article>
					</div>
					<br>
					<div class="container">
						<form id="form" action="/uc-submit?svrid=maintainer&path=<%= currentPage %>" method="post">
							<label class="label">PM Commands</label>
							<p class="control">
								<textarea name="pm_commands" class="textarea" ><%- JSON.stringify(config.pm_commands, null, 2) %></textarea>
								<span class="help">List of accepted command names in private message. Requires corresponding module in <code>Commands/PM/</code>.</span>
							</p>
							<label class="label">Commands</label>
							<p class="control">
								<textarea name="commands" class="textarea" ><%- JSON.stringify(config.commands, null, 2) %></textarea>
								<span class="help">List of accepted public command names . Requires corresponding module in <code>Commands/</code>.</span>
							</p>
							<label class="label">PM Command Usage Strings</label>
							<p class="control">
								<textarea name="pm_command_usages" class="textarea" ><%- JSON.stringify(config.pm_command_usages, null, 2) %></textarea>
								<span class="help">Usage help for PM commands. Shown in <code>help</code> command.</span>
							</p>
							<label class="label">Command Descriptions</label>
							<p class="control">
								<textarea name="command_descriptions" class="textarea" ><%- JSON.stringify(config.command_descriptions, null, 2) %></textarea>
								<span class="help">Text descriptions for public commands. Shown in admin console.</span>
							</p>
							<label class="label">Command Usage Strings</label>
							<p class="control">
								<textarea name="command_usages" class="textarea" ><%- JSON.stringify(config.command_usages, null, 2) %></textarea>
								<span class="help">Usage help for public commands. Shown in <code>help</code> command.</span>
							</p>
							<label class="label">Disabled Commands</label>
							<p class="control">
								<textarea name="disabled_commands" class="textarea" ><%- JSON.stringify(config.disabled_commands, null, 2) %></textarea>
								<span class="help">Public commands that are disabled in the default server config.</span>
							</p>
							<label class="label">Admin Commands</label>
							<p class="control">
								<textarea name="admin_commands" class="textarea" ><%- JSON.stringify(config.admin_commands, null, 2) %></textarea>
								<span class="help">Public commands that are marked admin-only in the default server config.</span>
							</p>
							<label class="label">Filter-Enabled Commands</label>
							<p class="control">
								<textarea name="filtered_commands" class="textarea" ><%- JSON.stringify(config.filtered_commands, null, 2) %></textarea>
								<span class="help">Public commands to which the NSFW filter (if enabled).</span>
							</p>
							<br>
							<% var formButtonsUnsaved = false; %>
							<% include ../partials/form-buttons %>
							<div id="form-buttons" style="display:none;background-color:#f5f7fa;position: fixed;padding: 20px;left: 50%;transform: translate(-50%, 0%);bottom: 0px;z-index: 100;width: 100%;">
								<div class="container">
									<% var formButtonsUnsaved = true; %>
									<% include ../partials/form-buttons %>
								</div>
							</div>
						</form>
					</div>
				</div>
			</div>
		</div>
	</section>

	<% include ../partials/footer %>
</body>
</html>